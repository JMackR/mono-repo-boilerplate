import styled from 'styled-components'

export const AvatarRemoveLink = styled.span`
  font-weight: 600;
  font-size: 16px;
  line-height: 20px;
  color: inherit;
  outline: none;
  &:hover {
    text-decoration: underline;
    cursor: pointer;
  }
`
